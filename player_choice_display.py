# take player input, compare with game selected word. If character matches then replace
# the _ with the matching letter.
# reduce player guess count value by 1. if value == 0, then display failure screen.